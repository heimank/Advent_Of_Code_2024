-- Two tables with the first and second value respectively + AUTOINCREMENT
-- ORDER BY 2nd column with the values 
-- JOIN on the AUTOINCREMENTID 
-- SUM(a.value - b.value)  

CREATE TABLE (
    id NOT NULL AUTOINCREMENT
)